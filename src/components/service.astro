---
const { item } = Astro.props;
---

<div
  class="rounded-md p-8 xl:p-10 ring-1 ring-gray-200 dark:bg-stone-950 dark:ring-gray-700 flex flex-col justify-between"
>
  <div class="flex items-center justify-between gap-x-4">
    <h3 class="text-lg font-semibold leading-8 text-gray-900 dark:text-white">
      {item.name}
    </h3>
  </div>
  <div class="mx-auto prose prose-lg dark:prose-invert dark:text-gray-200">
    <ul
      role="list"
      class="mt-8 space-y-3 text-sm leading-6 text-gray-600 dark:text-gray-300 xl:mt-10"
    >
      {item.features.map((item) => <li>{item}</li>)}
    </ul>
  </div>
</div>
